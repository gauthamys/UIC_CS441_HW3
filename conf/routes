# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

POST    /modelLocal         controllers.ModelController.generateLocalModel

POST    /modelExternal      controllers.ModelController.generateExternalModel

GET     /health             controllers.HealthController.health

POST    /conversation       controllers.AgentController.converse